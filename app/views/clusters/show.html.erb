<% set_meta_tags title: @cluster.name %>
<% set_meta_tags description: t("views.clusters.show.metas.description", cluster: @cluster.name, region: @region.name) %>
<% set_meta_tags og: { type: "website", image: image_url("hero-homepage.jpg") } %>
<% set_meta_tags canonical: cluster_url(@region, @cluster) %>

<div class="max-w-7xl mx-auto sm:px-6 lg:px-8 mt-8 pb-8">
  <nav class="flex mb-4 px-4 sm:px-0" aria-label="Breadcrumb">
    <ol role="list" class="flex items-center space-x-2">
      <li>
        <%= link_to regions_path, class: "text-sm font-medium text-gray-500 hover:text-gray-700" do %>
          <%= t("views.clusters.show.breadcrumbs.explore") %>
        <% end %>
      </li>
      <li>
        <div class="flex items-center">
          <svg class="h-5 w-5 flex-shrink-0 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
            <path d="M5.555 17.776l8-16 .894.448-8 16-.894-.448z" />
          </svg>
          <%= link_to @region.name, region_path(@region), class: "ml-2 text-sm font-medium text-gray-500 hover:text-gray-700" %>
        </div>
      </li>
    </ol>
  </nav>

  <h1 class="text-3xl font-bold mb-4 px-4 sm:px-0"><%= @cluster.name %></h1>

  <div class="px-4 sm:px-0 grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-6 gap-2 mb-8">
    <div>
      <%= link_to cluster_areas_levels_path(@region, @cluster), class: "min-w-max" do %>
        <%= use_case_card(title: t("views.areas.index.cards.level"), color: "bg-gradient-to-b from-blue-300 to-blue-400") do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 lg:h-8 lg:w-8 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
          </svg>
        <% end %>
      <% end %>
    </div>

    <div>
      <%= link_to cluster_top_areas_dry_fast_path(@region, @cluster), class: "min-w-max" do %>
        <%= use_case_card(title: t("views.areas.index.cards.dry_fast"), color: "bg-gradient-to-b from-yellow-300 to-yellow-400") do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 lg:h-8 lg:w-8 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
          </svg>
        <% end %>
      <% end %>
    </div>

    <div>
      <%= link_to cluster_top_areas_train_path(@region, @cluster), class: "min-w-max" do %>
        <%= use_case_card(title: t("views.areas.index.cards.train_html"), color: "bg-gradient-to-b from-pink-300 to-pink-400") do %>
        <% end %>
      <% end %>
    </div>
  </div>

  <h2 class="py-4 px-4 sm:px-0 text-2xl font-bold text-gray-900">
    <%= t("views.clusters.show.areas") %>
  </h2>

  <div class="lg:grid lg:grid-cols-2 lg:gap-16 mb-4">
    <div class="col-span-1">
      <div class="overflow-hidden bg-white shadow ring-1 ring-black ring-opacity-5 md:rounded-lg">
        <ul role="list" class="divide-y divide-gray-200">
          <% @areas_with_count.each do |area, count| %>
            <li>
              <%= link_to area_path(@region, @cluster, area), class: "block hover:bg-gray-50" do %>
                <div class="px-4 py-4 sm:px-6">
                  <div class="flex justify-between">
                    <p><%= area.name %></p>
                    <div class="flex items-center space-x-2 text-gray-400">
                      <span><%= count %></span>
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
                      </svg>
                    </div>
                  </div>
                </div>
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
